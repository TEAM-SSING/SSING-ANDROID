package com.ssing.presentation.consumerhome.model

internal enum class Sports(val label: String) {
    SKI("SKI"),
    SNOWBOARD("SNOWBOARD")
}

internal enum class DisplayStatus(val label: String) {
    CONFIRMED("CONFIRMED"),
    IN_PROGRESS("IN_PROGRESS")
}